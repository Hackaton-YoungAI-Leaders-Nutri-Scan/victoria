import { BACKEND_BASE_URL as ENV_BACKEND_BASE_URL } from '@env';

// Envuelve la variable de entorno para tener un lugar único de importación en el código
export const BACKEND_BASE_URL = ENV_BACKEND_BASE_URL;
